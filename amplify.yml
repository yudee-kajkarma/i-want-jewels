version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --cache .npm --prefer-offline
    build:
      commands:
        - env | grep -e NEXT_PUBLIC_API_BASE_URL >> .env
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
      - '!cache/**/*'
  cache:
    paths:
      - .next/cache/**/*
      - .npm/**/*
